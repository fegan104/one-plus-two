export default ({
  id,
  desc,
  event,
  additionalInvitesLeft,
  isUsed,
  user,
  checkedInAt,
  ...rest
}) => ({
  id,
  desc,
  event,
  additionalInvitesLeft,
  isUsed,
  user,
  checkedInAt
});
