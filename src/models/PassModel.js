export default ({
  id,
  desc,
  event,
  additionalInvitesLeft,
  isActive,
  isUsed,
  user,
  checkedInAt,
  ...rest
}) => ({
  id,
  desc,
  event,
  additionalInvitesLeft,
  isActive,
  isUsed,
  user,
  checkedInAt
});
